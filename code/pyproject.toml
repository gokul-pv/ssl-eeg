[project]
name = "code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mne>=1.11.0",
    "mne-bids>=0.17.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
